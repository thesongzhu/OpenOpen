# Provenance Map

## Current Choice Loop provenance status — 2026-07-19

Current product authority is the private-agent Choice Loop in
`docs/OPENOPEN_BUILD_WEEK_MASTER_PLAN.md` and, after same-fingerprint review,
its companion documents. This map records source adaptation only; it cannot
select product behavior.

The protected Hero route is PR1 Choice Core+Mac followed by PR2 iMessage
same-account self-chat and a same-main Core+iMessage checkpoint App/DMG. Minimal
B2 and minimal C2 follow as narrow proof chapters in the final B+ package. It introduces no
newly approved third-party source or model-specific prompt dependency.
Compatible account models receive the same bounded semantic brief. Existing
Friday-derived Mission/Store/audit and effect-safety concepts remain subject to
their recorded licenses and tests. One additional read-only iMessage source,
broader B2/C2, and PR3 Discord are post-B+. Their
isolated owned-path preparation does not authorize shared integration or real
provider/Skill actions. The B2 local diagnostic is read-only, no-network,
no-retention, and non-gating; the supplied real export and all path/hash/member/
content/derived metadata stay outside the repository, evidence, and remote.
Any bounded history excerpt reaches the selected OpenAI model only after later
exact Owner consent. Claude/Anthropic paths remain excluded.

The scheduling policy adds no third-party dependency: Host grounds Reminder
date/time/timezone only in explicit user temporal input, requires selection
when time is absent, and binds every exact future schedule edit to a new
confirmation revision. Real EventKit write remains separately authorized.

## Friday

- Upstream: `https://github.com/thesongzhu/Friday`
- Source commit: `4870f31fa088bef7eb9f4f256ec62993b02eda80`
- License: MIT, copyright Friday contributors

Only the smallest tested concepts or implementation portions needed by
OpenOpen may be adapted. Every adapted file must be added to this table before
staging.

| OpenOpen module | Friday source | Adaptation | Verification |
| --- | --- | --- | --- |
| `crates/openopen-core/src/mission.rs` | `rust-core/crates/friday-core/src/mission.rs` | Independent OpenOpen lifecycle adapted from Friday's legal-transition and proof-before-completion invariants | bootstrap `19ecdd9…`; two isolated foundation reviewers PASS; local 95-test verification; follow-up PR run `29370433505` PASS on synthesized merge `d502b3d…`, whose tree equals head `923c88a…`; exact-head and release proof pending |
| `crates/openopen-core/src/store.rs` | `rust-core/crates/friday-storage/src/mission.rs`, `rust-core/crates/friday-storage/src/audit.rs` | Independent encrypted lifecycle persistence and signed/tail-anchored audit design | bootstrap `19ecdd9…`; two isolated foundation reviewers PASS; local 95-test verification; follow-up PR run `29370433505` PASS on synthesized merge `d502b3d…`, whose tree equals head `923c88a…`; product-shell `e2313fe…` passes local 134-Rust/67-Swift verification, two fresh Repair20 reviewers PASS fingerprint `29a00413…`, and PR #2 run `29386477267` PASS on synthesized merge `487dae1…`, whose tree equals the head tree; the first Hero A candidate passed 139-Rust/70-Swift locally but failed both closure reviews; Repair1 passed governance but failed functional review on physical EventKit target drift; both Repair2 reviewers failed its first-write/cancellation/all-markers-missing recovery boundary; both Repair3 reviewers then failed frozen `fa9d905e…` because volatile `createOnce` authority survived possible EventKit commit; Repair4 added durable signed dispatch Evidence but governance rejected fingerprint `4cabaeb4…` because its lower-level writer still accepted reusable Mission copies; Repair5 consumes an internal one-shot dispatch start before any external boundary, passes 146 ordinary Rust plus 83 Swift tests and ad-hoc staging locally, and receives two fresh reviewer PASS reports on frozen fingerprint `4b41a04f…`; Hero A commit `774789c…` is pushed and Actions `29393462659` passes on equal-tree synthesized merge `bccdf360…`; exact-head, signed/admin, cross-UID, provider, and release proof remain pending |

The following paragraph records the former competition support plan; it cannot
redefine the current Choice contract. Workflow, Deep ZIP, and GitHub Skill
adaptations are not present or claimed on the clean Choice baseline. The
historical approved competition pins are
`queelius/ctk@99784b7582a583fbae0725a5288797739dc347dd`,
`slyubarskiy/chatgpt-conversation-extractor@b7c4372b518a006df57415b0d4287fbbdf88ed29`,
and OpenClaw Skill design/test semantics at
`openclaw/openclaw@af62abeeef86046daaa284d2eb6eef814aec11f7`.
No file or fixture from those sources may be added to this map until its exact
license, source path, adaptation, and verification are known. OpenClaw Gateway,
ClawHub, Python/Node memory runtimes, and arbitrary script execution are not
approved dependencies.

No Friday Hub, provider, TypeScript execution route, mobile application, or UI
is imported.

The Repair4 Codex runtime-home boundary is original OpenOpen integration code,
not a Friday or third-party adaptation. It invokes only macOS's system
`mount_tmpfs` helper at its exact SIP-owned path, adds no distributed
dependency, and does not copy Keychain credentials. The signed Repair5
replacement is installed with exact staged/installed file-manifest equality;
Repair6 real root-broker mount/device/flags/capacity/hard-link proof passes.
Repair7 replacement2 then passed two fresh reviewers, was installed, launched
the exact root broker and bounded tmpfs, and reached real provider OAuth before
the login-only sandbox denied the Keychain lock protocol. Repair8 adds only the
exact Keychains-parent metadata and `.fl<hex>` lock-file reads to that
short-lived login process; the model profile and every non-login write remain
unchanged. Its synchronized package passed product review but failed security
review on one P1 because Core retained a numeric-PID `Child::kill()` route after
the exact broker lease was installed. Repair9 permanently removes that
post-lease signal authority from Core: Core closes stdin and reaps only, while
same-Core rotation asks the broker to terminate the exact old Codex Mach
audit-token incarnation before releasing the old lease. The synchronized
Repair9 candidate passed deterministic verification, but its fresh reviewers
found a retry wedge after local lease retirement and an ambiguous process-
inspection failure that could be mistaken for death. Repair10 allows only an
uninitialized candidate to bridge the recoverable old-lease state, keeps every
operation gated until the new signed lease is installed, and makes broker
liveness explicitly alive/dead/inspection-failure with unknown always rejected.
Complete deterministic verification passes; two fresh reviews, install,
account/models, integrated Mission, and final release proof remain pending.
Repair10 product review passed, but its fresh security reviewer found that a
broker-acquire or Core-install response loss could occur before Core marked the
candidate lease-bound. Repair11 makes that handoff an explicit one-way Core
step before broker acquisition; every later abort/drop is pipe-close/wait-only,
whether broker acquire or Core install succeeds, fails, or loses its response.
Repair11 was installed and its real official OAuth/MFA flow completed, but
credential persistence failed closed when Seatbelt denied Security.framework's
atomic-save sidecar creation at the exact
`login.keychain-db.sb-<8 hex>-<6 alphanumeric>` shape. Repair12's create-only
rule was insufficient: isolated review reproduced the next required atomic-save
`file-write-mode` denial before commit. Repair13 permits only the exact
sidecar's create, mode, owner, flags, times, and unlink operations in the
login-only profile. It deliberately omits sidecar `file-write-data` and generic
Keychains-directory writes and does not alter the model profile. A disposable
Security.framework Keychain test now proves a complete sandboxed save/readback
and cleanup; a pre-created matching hard link cannot be content-written or
renamed over the Keychain database. Same-UID metadata operations on such a
pre-created link are possible but add no content-write authority and are
recorded explicitly. Complete deterministic verification and a synchronized
Developer-ID owner-test package pass. Review status is deliberately absent from
this embedded document and exists only in external task/PR evidence; the
package never self-certifies review. It remains unnotarized and claims no real
account/model/provider, milestone, or release proof. Repair12 is likewise not
release proof.

Repair14 is original OpenOpen integration code and adds no dependency. It
replaces the singular Mission channel origin with a Mission-bound route set:
one atomic primary route at genesis and only explicitly owner-approved durable
paired additional routes thereafter. Additional outbound classes default Off.
Bound inbound is a typed Mission participation event, never a free Outcome,
scope grant, Mission creation, or completion Evidence. Exact route-set
revision, pairing, owner, provider identity, and message-class authority govern
each outbound; Reminders readback remains the only Friday completion Evidence.
The Store atomically binds each durable event to a Store-verified typed Mission
participation command that can advance only the existing Mission revision and
timestamp; it cannot decide a pending approval, change scope, or attach
Evidence. Swift exposes only the correlated event metadata after rechecking the
exact Mission, route, and channel. A duplicate/restart may recover an immutable
historical event only when its route-set revision is not in the future and the
exact route already existed at that revision; unknown, pre-route, and future
events fail closed. Terminal Mission routes do not capture later explicitly
addressed input from the same paired conversation, so the established one-route
Outcome path remains reusable.
The legacy singular schema is migrated once to one primary route and then
removed from production reads and writes. Complete deterministic verification
passes without changing the Repair13 broker trust, runtime-home, or Keychain
sandbox contract. This document does not claim package review, installation,
real provider closure, a milestone, notarization, or release proof.

The Repair7 login-only Keychain sandbox and same-App/Core Codex lease rotation
are also original OpenOpen integration code. They add no dependency and do not
read, copy, parse, export, or log credential bodies. The exact pinned official
Codex `0.144.0` app-server remains the sole login and model runtime; the only
new authority is a short-lived outer-sandbox write grant to the canonical
login Keychain database plus exact create/mode/owner/flags/times/unlink access
to Security.framework's atomic-save sidecar shape during official managed
login, followed by mandatory destruction and a fresh read-only model process.
No separate sidecar `file-write-data` predicate exists, though creation
necessarily permits Security.framework to write the newly created file through
its fresh descriptor. Existing and pre-created-hardlink content writes and
rename-over-database, invalid names, sibling Keychain files, and every
model-process write remain denied; same-UID metadata-only effects on a
pre-created matching link are explicitly not claimed as denied.

The local Friday working tree was observed detached at `25329515…` during this
phase. Its `origin/main` later advanced to
`0871c37a5f88f30371ad86fa9583fe24c09ef253`; the required pin remains an
ancestor. All provenance inspection and adaptation uses the immutable form
`git show 4870f31fa088bef7eb9f4f256ec62993b02eda80:<path>` and does not import
later upstream changes.

## Other sources

### Friday Discord adapter contract (implemented Rust port; provider proof pending)

- Exact source: Friday commit
  `4870f31fa088bef7eb9f4f256ec62993b02eda80`
- License: MIT
- Relevant paths: `src/channels/friday-channel-adapters.types.ts`,
  `src/channels/discord/discord-config.schema.ts`,
  `src/channels/discord/friday-discord-channel.ts`,
  `src/channels/discord/discord-service.ts`, and the matching dedupe,
  reconnect, adapter, and live-roundtrip tests.
- Allowed adaptation: generic envelope, user/channel allowlists, explicit
  mention gating/stripping, bot filtering, message-ID TTL dedupe,
  reconnect/status, safe `allowed_mentions`, and live roundtrip/restart test
  semantics ported into Rust. The Friday TypeScript/Node runtime is excluded.
- OpenOpen paths: `crates/openopen-discord-adapter/src/lib.rs`,
  `crates/openopen-host/src/channels.rs`, and the shared typed channel records
  in `crates/openopen-protocol` and `crates/openopen-core`.
- Verification: 19 focused adapter tests cover the token-derived identity,
  official `scope=bot` install URL and permission bits `101376`, 128-bit
  pairing code, Ready/intent gate, live permissions/history probe, explicit
  candidate binding, bot/DM/code rejection, identity drift, operational
  traffic, reconnect, dedupe, and recovery. The historical Repair2 local suite
  passed 187 Rust and 91 Swift tests before its replacement-review failure;
  the current Repair3 tree passes 190 Rust and 93 Swift tests. Two fresh
  reviewers PASS fingerprint `3e201547…`; implementation commit `2685b57…`
  is pushed, and PR #2 Actions run `29440208503` passes on synthesized merge
  `99ee2b10…`, whose tree equals the exact head tree. Real Discord
  roundtrip/restart proof remains pending.

### imsg (implemented basic-RPC adaptation; provider proof pending)

- Upstream: `https://github.com/openclaw/imsg`
- Version/tag: `v0.13.0` (annotated tag object `1677a9fe…`)
- Exact dereferenced commit:
  `fa2f82d7dbda4c802d91c1d41bb6c53564ed2fdc`
- License: MIT, copyright 2026 Peter Steinberger
- Allowed surface: one host-managed basic JSON-RPC/stdio child using
  `chats.list`, scoped history/watch, `send`, and `message.send_status`.
  IMCore/private bridge, advanced private operations, SIP changes, and TCP
  daemon/server surfaces are excluded.
- OpenOpen paths: `crates/openopen-imsg-adapter`,
  `third_party/imsg/openopen-basic-rpc.patch`,
  `third_party/imsg/Package.resolved`, and `scripts/build_pinned_imsg.sh`.
- The tracked patch reduces the CLI to `rpc`, uses positive compile-source
  manifests (25 `IMsgCore` and 13 CLI sources), reduces dispatch to the six
  approved methods, and requires selected-chat plain-text AppleScript sends.
  The build script verifies the exact upstream commit, patch, dependency lock,
  source manifests, static forbidden markers, version/help surface, and emits
  schema-v2 build and resource receipts. It never compiles or stages
  `IMsgHelper/IMsgInjected.m`, IMCore/private bridge helpers, SIP changes, or a
  TCP service.
- Historical local integration: the former 8 focused Rust adapter tests and two upstream
  OpenOpen boundary tests passed. A receipt-bound binary with artifact SHA-256
  `626439fbf79a8b7a44bc189c088cda2f9d4c53d5776dfe404f6a65dd83a5fc11`
  is staged at `Contents/Resources/iMessage/0.13.0/bin/imsg` in
  `/private/tmp/OpenOpen-FridayAlpha-Final.app`, which reports
  `STAGED_AD_HOC_NOT_RELEASE_PROOF`. The resulting ad-hoc, unnotarized DMG
  passes read-only mount/copy/signature install testing; its SHA-256 is
  `0f9b7fd3ca54c27138c52fe42a0cb31a3a4a13260d0d945a954d608cab39bd15`.
  That package is now historical only: both current-tree reviewers rejected
  its pre-sign-only runtime binding and found private IMCore/SIP/bridge code
  still compiled into the Mach-O, while a follow-up audit found the locked
  `PhoneNumberKit_PhoneNumberKit.bundle` absent from the App and referenced
  only through a deleted temporary build path. Repair must use an explicit
  compile-source whitelist, a signed-runtime receipt plus running-child
  identity validation, and a receipt-bound resource tree. Repair1 implemented
  those boundaries: 10 focused Rust adapter tests passed; the build receipt
  bound the three-file runtime tree and resource tree `7a5cb869…`; Host prepared
  the child without sending RPC bytes, the App validated its exact running Mach
  identity, and only a second proof-bearing RPC activated the watch. Its
  replacement reviewers then rejected frozen fingerprint `10160bb1…` because
  Host handed the already-prefixed approved wire body to an adapter that
  requires unprefixed content and the patched send result did not return a real
  provider identity. Repair2 preserved the audited final wire body and added a
  bounded read-only restart scan, but both replacement reviewers rejected
  frozen fingerprint `1a983c72…`: a prior Mission's same-text GUID could be
  misbound to the current outbound, prepare response loss could wedge retry,
  and the UI still required database IDs instead of an explicit conversation
  selection route. Repair3 never promotes any history scan to `Sent`; only the
  exact synchronous send RPC may return a provider GUID. The patched sender
  sends once, accumulates candidates for the complete two-second window, and
  keeps any multi-candidate observation permanently ambiguous. Conversation
  discovery is a separate prepare/validate/list flow: prepare sends no RPC
  bytes before exact running Mach identity validation, list consumes a second
  fresh proof, returns only bounded exact `service == "iMessage"` chats, and
  always shuts down its child. Global Off, explicit stop, activation failure,
  and prepare response loss also clear the prepared child. Swift exposes those
  chats and participants for explicit selection instead of free-form database
  identity entry.
- Repair3's fresh pinned build passes five upstream OpenOpen RPC tests. Its
  unsigned binary SHA-256 is
  `635c99814fc3dbefffacaeb5222d4bf2ed340d019e726751ead909addc9122a1`,
  build receipt SHA-256 is
  `c1769b4093faa6e8bde56cdb16ad2c950ee39ea5501630e0ba022901b56a7b3d`,
  and resource tree remains `7a5cb869…`. The complete local tree passes 190
  ordinary Rust tests with one explicit environment-gated Codex test, 40
  broker/signing plus 53 App Swift tests, release/strict lint/format checks,
  two explicit pinned Codex diagnostics, and an independent 597-text notice
  closure check. Two fresh replacement reviewers PASS fingerprint
  `3e201547…`; implementation commit `2685b57…` is pushed, and PR #2 Actions
  run `29440208503` passes on equal-tree synthesized merge `99ee2b10…`.
  Evidence follow-up `becea456…` passes run `29442001103`. The subsequent
  local signing slice gives the pinned sender only
  `com.apple.security.automation.apple-events`, signs it and every OpenOpen
  executable with hardened runtime, a secure timestamp, and Team
  `UHDY2275L5`, and records that Team/CDHash in its runtime receipt. It first
  verifies the upstream Codex `rg` SHA-256 `4fdf1d83…`; because that upstream
  nested executable is linker-ad-hoc, Developer-ID mode re-signs the same
  Mach-O with OpenOpen Team/hardened runtime/timestamp and records upstream and
  runtime hashes plus Team/CDHash in `CODEX-RUNTIME-RECEIPT.json`. The signed
  local v3 app and DMG passed every-Mach-O structural verification, but the
  first signing-slice governance reviewer rejected frozen fingerprint
  `eaa4bc2e…`: staging still trusted a caller-supplied imsg receipt, the DMG
  creator accepted a same-Team renamed app without exact nested-code and
  receipt binding, and the README omitted the mandatory pinned inputs. v3 is
  therefore historical and not an alpha candidate. The first repair pins the exact
  schema-v2 build receipt, unsigned imsg bytes/size, patch, runtime/resource
  trees, compiled-source manifests, and the three-file runtime allowlist before
  any build or signature. DMG creation additionally requires the exact
  OpenOpen bundle/signing identifiers, eight-Mach-O allowlist, Apple anchors,
  Teams, hardened runtime/timestamps, entitlement split, runtime receipts,
  upstream hashes, and frozen CDHashes. Local v4 satisfied that focused contract,
  but both fresh v4 reviewers rejected unchanged fingerprint `08a58745…`: the
  App verifier omitted modes, the DMG accepted any generic same-Team Developer
  ID leaf rather than the exact owner certificate, and staging lacked complete
  post-sign/final-copy unsigned-content checks. v4 and DMG SHA `feec94d3…` are
  historical, not signing PASS evidence. The replacement repair pins owner leaf
  SHA-256 `a7e43925…`, binds the exact directory/file/type/mode allowlists, and
  checks every owned Mach-O before and after signing and at final output. A
  process already authorized to use the owner's signing identity can call
  `codesign` directly; output verification does not claim otherwise. Fresh
  Focused replacement verification now passes exact owner-leaf checks on every
  owned component and the DMG; all 18 directories are `0755`, the eight Mach-O
  files are `0755`, and every other regular file is `0644`. Main-executable,
  resource-file, directory-mode, extra-directory, and wrong-identity negatives
  all fail before output, while the ad-hoc diagnostic route still stages
  successfully. The provenance-embedded replacement candidate satisfies this
  focused contract. Fresh functional and governance reviewers both PASS frozen
  fingerprint `fdf5a00e8c0c4ca92ab4ff8e9cf33041c801bac5e74304bc484c613c35e33235`
  with zero P0/P1/P2 after independently checking the original, mounted, and
  copied App plus the focused negative routes. That first closure was
  historical; the provenance-bound Final2 replacement later received two
  entirely fresh PASS reports on unchanged fingerprint `026b2b1f…`, was
  committed and pushed as `5a461ef…`, and passed PR #2 Actions run
  `29450863581` on equal-tree synthesized merge `da3d7d1…`. Reviewer results
  remain task/PR evidence and are not recursively embedded into the reviewed
  package. These focused packaging facts are not a substitute for the recorded
  full suite. Full Disk Access, Messages Automation, real bidirectional
  traffic, notarization, and release proof remain pending.

- Repair15 records a real, bodyless discovery diagnostic against the exact
  installed basic-RPC adapter after official Full Disk Access was enabled for
  OpenOpen. `chats.list` returned 23 unique positive-ID `service=iMessage`
  rows; 21 legitimate rows had an empty optional display name. Host now accepts
  only that empty-name shape through the existing bounded, NUL-free, trimmed
  validator and preserves the empty value for Swift's participant fallback.
  Participant presence/count/field validity/uniqueness, service filtering,
  positive unique IDs, sorting, pairing, and scope remain unchanged. No
  message body, conversation label, participant value, token, send, or pairing
  is recorded or claimed. This changes no upstream pin, bundled source,
  license, broker, Keychain, sandbox, or recipient authority. The complete
  Repair15 matrix passes 220 executed Rust tests with two explicit real-runtime
  diagnostics ignored, 56 broker/signing Swift tests, 72 App Swift tests, and
  release/strict/static checks. Package and reviewer status exists only in
  external task evidence bound to an exact fingerprint; this embedded document
  does not self-certify or dynamically claim that status.

- Repair16 changes no dependency, provider adapter, pin, entitlement, access
  group, broker, sandbox, or recipient scope. It removes the unsupported
  `kSecUseDataProtectionKeychain` selector from the original OpenOpen Discord
  token store so direct Developer-ID distribution uses the same single native
  login Keychain backend already established for broker security data. The
  production service remains `com.thesongzhu.OpenOpen.Discord`, the account
  remains `official-bot-token`, and insert accessibility remains
  `AfterFirstUnlockThisDeviceOnly`. No fallback exists. Tests use only unique
  disposable service/account names and bounded synthetic values; they never
  touch or read the real token item. The approved real token attempt failed
  before Discord provider access and left the production item absent. No token
  value is recorded in source, logs, evidence, or a temporary file. Its first
  pre-install candidate is historical: the security reviewer passed, but the
  product reviewer rejected false connected feedback during Discord's real
  initial `Connecting` state and mutable iMessage selection controls after
  durable pairing. The replacement follows the actual connection states,
  never renders a pending or failed state as connected, and locks iMessage
  discovery and selection while connected. Its Discord start path also rejects
  any late result or failure after the Global Off generation changes, so Off
  cleanup cannot be overwritten by stale feedback or polling. The same
  generation fence is checked immediately after iMessage activation and every
  channel poll await, before any connection status, feedback, Mission event, or
  Outcome suggestion can mutate UI state. A stale iMessage cleanup callback
  cannot stop a newer generation. Deterministic delayed-activation and
  non-cooperative delayed-poll regressions prove Global Off remains
  disconnected, publishes no late event or suggestion, and starts no
  continuing poll. Discord start is idempotent only for the exact durable
  pairing: if the provider session was created but the RPC response was lost,
  retry reattaches to that same session and reports its current state without a
  second provider session; any changed pairing fails closed. A launch-pending
  marker distinguishes that response-loss window from a terminal provider
  state. An exact-pairing retry keeps every connecting, connected, or
  reconnecting session, but first stops and replaces one terminal faulted
  session or one launch-complete disconnected session. The replacement starts
  no outbound effect and cannot coexist with the stopped session. If the preceding
  owner-confirmed pairing commit succeeds but its RPC response is lost, Swift
  accepts recovery only from the durable pairing whose owner, conversation,
  provider identities, setup message, and candidate all exactly match the
  confirmed candidate, then clears the obsolete setup UI and starts that one
  route. Restart recovery is also fail-closed at the Host/Store boundary:
  while a bounded Discord history batch is pending, the session reports only
  connecting and exposes no outbound handle. Each recovered inbound or final
  high-water cursor remains the exact outstanding event until its atomic Store
  write succeeds; a failed write or lost reply repeats that same event rather
  than advancing the in-memory queue. Accepted recovered messages may be
  durably queued, but no queued Discord model work is claimed, started, or
  surfaced until every recovered event and the final provider high-water cursor
  are durably accepted. A later review proved that cursor closure alone was
  insufficient: the oldest queued result could still become visible while a
  later correction remained queued. Recovery arbitration now withholds every
  channel suggestion while queued or started work remains, accepts confirmation
  only for the newest ready suggestion after the channel is recovery-ready, and
  restores only that exact suggestion after Host restart or response loss.
  Swift clears a displayed channel result when recovery resumes and replaces it
  only with the final Host-arbitrated result. The final GPT request receives a
  bounded chronological correction context only after strict per-channel FIFO:
  one started dispatch blocks every later queue claim, and atomic begin accepts
  only the oldest queued source. The Store first selects the immediately
  preceding accepted observation, then separately requires that exact dispatch
  to be ready and audit-qualified; it never skips an unresolved predecessor for
  an older ready result. The Host independently rejects caller-assembled
  contexts above two messages. The request receives a
  bounded chronological correction context only when the approved owner starts
  the latest message with the exact case-insensitive directive `Correction to
  previous:` and the Store proves that the one immediately preceding
  same-sender/same-conversation result completed after the later message was
  already durably observed. Time overlap alone never grants context. The
  context is capped at exactly two messages and remains subject to the existing
  16 KiB prompt contract; invalid or changed context fails closed, while an
  unmatched directive remains a single-message request. Unrelated earlier
  intents are never imported. If the Host exits after consuming the exact
  one-shot model authority but before persisting a result, restart selects only
  that single durable started source before any queued work and returns it as
  recovery-only `Need you`; it never grants a second model call. Multiple
  started rows fail closed. Swift surfaces the local pause and stops automatic
  polling for that channel rather than faulting forward into later work. A malformed or failed
  recovery stops and removes the exact provider session so an unchanged durable
  pairing can start one clean replacement, while Global Off removes all pending
  recovery state.
  This changes no provider, dependency, Keychain, broker, sandbox, recipient,
  completion-Evidence, or model authority. Package and reviewer status exists only in external task evidence
  bound to an exact fingerprint; this embedded document does not self-certify
  or dynamically claim that status.

- 2026-07-17 Repair16 final15 changes only the local Swift presentation of the
  already provider-derived Discord pairing instruction. Installed final14
  reached the official Gateway, but SwiftUI's localized interpolation inserted
  grouping commas into the 19-digit `UInt64` bot snowflake. Final15 constructs
  the instruction from `String(botUserId)` and renders it verbatim, preserving
  text selection and the provider-issued pairing code. It adds no dependency,
  provider, token, recipient, Keychain, broker, sandbox, model, or effect
  authority. A grouping-locale regression and the complete
  233-executed-Rust/56+87-Swift release/strict matrix pass locally. Package and
  review status remain external fingerprint-bound evidence; this document does
  not self-certify either.

- 2026-07-17 Repair17 retains the final15 verbatim pairing instruction and
  removes the always-mounted Discord `SecureField` from the ordinary
  Connections page. Token entry exists only in an explicitly opened,
  auto-focused secure sheet; its submit path uses the unchanged
  `DiscordTokenKeychain` validation/save and official setup route, while
  cancel, dismissal, submit, and Global Off erase the ephemeral draft. Stable
  accessibility identifiers cover only controls, never secret content. No
  clipboard, environment import, CLI insertion, second Keychain backend,
  provider, recipient, broker, sandbox, model, or effect authority is added.
  The corrected real Discord pairing command was sent once through Discord's
  official mention picker, but durable product confirmation and all provider
  proof remain external pending evidence. Package and review status remain
  external fingerprint-bound evidence; this document does not self-certify
  either.

- 2026-07-17 Repair18 adds no dependency, provider, token, Keychain, sandbox,
  broker, recipient, model, or effect authority. It repairs only App↔Core
  process supervision after real installed evidence showed a cancelled SwiftUI
  request closing the shared Core stdin, followed by stale provider
  presentation. Per-request cancellation now abandons only the exact response;
  one typed lifecycle event invalidates cached state and drives a bounded,
  non-overlapping exact-Core restart that restores only the two durable paired
  routes. Its first frozen package was rejected before install by both fresh
  reviewers with P0/P1/P2=`0/2/1`: cleanup could create uncounted Core
  generations, Global Off did not fence recovery/startup listener RPCs, and
  cancellation-before-write lacked a deterministic regression. The replacement
  cleanup never launches Core, every failed recovery attempt terminates and
  verifies the exact Core generation, and Global Off interrupts already-written
  noncooperative work. That final2 package was invalidated before install by
  product P0/P1/P2=`0/1/0` and security `0/1/1`: one attempt could stitch two
  silently replaced Core generations, and running validation independently
  recaptured the later termination identity. Final3 fenced each complete
  recovery and retained one captured audit token, passed its deterministic
  matrix, and produced App manifest `e55269d3…` plus DMG SHA `856f5403…`; both
  fresh final3 reviewers invalidated it with P0/P1/P2=`0/1/0`. Startup still
  performed provisioning/dashboard/Codex RPCs before entering the fence, and a
  post-launch validation/bootstrap failure could forget a child whose exact
  termination was rejected or not yet observed. Final4 tracks and fences the
  entire startup restore from its first Core RPC. It forgets a failed child only
  after same-token termination and exact exit; otherwise the child is
  quarantined without input and blocks replacement launch. Three failures pause
  with `Need you`. The OpenOpen-owned
  nonsecret canonical Discord identity file has
  SHA-256 `6a92c29f6a584fe9c103f0db1e24a7d110182cec0c76f72d694218c321e7bc81`;
  the credential remains only in Keychain. All 56 broker/signing and 108 App
  tests plus the 233-executed-Rust suite (two explicit real-runtime diagnostics
  ignored), both release builds, strict lint/format, locked metadata, notices,
  plist/scripts, diff, conflict, and credential-shape checks pass locally.
  Package and review status remains external fingerprint-bound evidence; this
  document does not self-certify either.

- 2026-07-17 Repair19 adds no dependency, provider, recipient, token, Keychain,
  sandbox, broker, model, or effect authority. The first real installed
  Repair18 structured turn exposed OpenOpen's use of unsupported JSON Schema
  `uniqueItems`; the pinned Codex child exited and the durable dispatch remained
  `started`. Repair19 removes only that keyword, retains duplicate-source
  validation after parsing, adds a signed atomic `failed` dispatch state, and
  requires an explicit correction instead of replaying claimed model work.
  Swift keeps correction polling live and performs the existing bounded exact-
  Core recovery. A final1 loaded verification exposed a narrow Core-exit race;
  final2 revoked the shared input slot before waking callers. Two fresh final2
  reviews still rejected that candidate before install: Global Off could race a
  late signed model result, an RPC installed before EOF retained its captured
  input handle, and current package/status prose was stale. Final3 holds the
  active-operation gate through reconciliation, requires the signed runtime to
  remain On inside the same immediate Store transaction, and serializes EOF
  revocation with the request's final pre-write authorization. Fresh final3
  product review then invalidated that package with P0/P1/P2=`0/1/0` while
  security passed `0/0/0`: successful Core recovery cleared the transient
  warning and the restarted Host did not rediscover the durable failed row.
  Final4 persistently recovers the newest failed dispatch as `Need you` until a
  later explicit correction supersedes it, without replay or another model
  call. Both final4 reviewers then reported one P1: failure feedback starved the
  provider poll required to ingest the correction, and a failure on one channel
  could clear another channel's valid Outcome. Final5 polls transport before
  persistent failure feedback and identifies only the exact same-channel
  suggestion for invalidation. Provider corrections become durable without
  replay; cross-channel suggestions remain isolated. The current
  236-executed-Rust and 56+111 Swift suites pass. Candidate and review identity
  is deliberately external exact-fingerprint evidence, so this byte-identical
  embedded document neither self-certifies nor self-invalidates packaging.
  Reviewed installation and the real integrated Mission remain required.

- 2026-07-18 Repair20 adds no dependency, provider, recipient, token, Keychain,
  sandbox, broker, Store, model, or effect authority. Installed Repair19
  final5b durably terminalized the sole consumed model dispatch without replay
  or external effect, but official Global Off then failed after exact old-Core
  shutdown because the replacement Host had no volatile broker enrollment and
  attempted to validate signed runtime history before enrollment. Repair20
  installs the already pinned exact broker enrollment first only after the old
  Core's exact shutdown is positively proven, then prepares the protected Off
  transition through the unchanged Core/Store/broker contract. The ordinary
  live-Core path still prepares Off before provisioning; rejected exact
  shutdown performs no prepare, provision, broker apply, Store write, model
  call, or provider work. The first replacement review pair invalidated final1:
  one missing retry regression and one false-On supersession race. The current
  repair retains quiescence until protected Off commits, then restores an exact
  On generation only after Codex, account/models, and both durable listeners;
  a newer Off interrupts that restoration and finishes protected Off. Both
  fresh final2 reviewers rejected that package with P0/P1/P2=`0/2/0`: a refused
  exact Core terminator was not retried on the same captured audit token, and
  the UI could publish On while listener restoration was still incomplete.
  Final3 quarantines the exact Core generation and retries its same audit token,
  but fresh review found that ordinary persisted Off→On bypassed the special
  restoration latch and that Discord `connecting`/`reconnecting` counted as
  restored. Final4 applies one complete generation-fenced restoration to every
  production-lifecycle protected On and requires bounded read-only proof of
  exact Discord `connected`. Terminal state or timeout pauses fail closed, and
  a newer Off interrupts the wait without replay. Nine focused regressions bind
  those roots plus the earlier failed-shutdown, exact-terminator, and On/Off
  supersession paths. Both fresh final4 reviewers invalidated that package:
  product found transient Unknown during Core-death recovery, and security
  found that protected On lacked exact managed-account/model readiness while
  Host model work lacked exact Discord Connected gating. Final5 keeps recovery
  Turning On, requires fresh ChatGPT plus `gpt-5.6-sol`/`high` before On/model
  entry, and requires exact Discord Connected for both model and outbound work.
  Missing account/model readiness never prevents protected Off. One Host guard
  still proves protected history requires exact re-enrollment. The current
  238-executed-Rust and 56+125 Swift suites pass. No
  dependency, provider, recipient, token, Keychain, sandbox, broker, Store,
  model, or effect authority changed. Package and reviewer identity remains
  external exact-fingerprint evidence; the real integrated Mission remains
  required.

- 2026-07-18 Repair21 adds no dependency, provider, recipient, token, Keychain,
  sandbox, broker, Store, model, or effect authority. Repair20 final5 passed its
  complete matrix and two fresh reviews and was installed, but the bounded
  recovery reached `.paused`. The single official Off attempt failed closed
  because `.paused` was omitted from the existing quiesced replacement-Core
  condition; the replacement Core therefore prepared protected history before
  receiving pinned broker trust. Repair21 adds only `.paused` to that existing
  condition. It reuses the same exact shutdown proof, cancellation, trust
  provisioning, prepare, and broker-signed monotonic commit path. Focused tests
  prove one legitimate Off commit and prove shutdown, provision, and prepare
  failures publish no false Off and perform no Mission/Receipt/outbound/model/
  provider work. The complete matrix passed 238 executed Rust tests with two
  explicit real-runtime diagnostics ignored and 56+127 Swift tests. Exact App
  manifest `509ee4b7…` and DMG `02964b91…` passed two fresh P0/P1/P2=`0/0/0`
  reviews, were installed through official ServiceManagement/XPC, and committed
  protected Off at revision 28 with both pairings and zero external work.

- 2026-07-18 Repair22 adds no dependency, provider, recipient, token, Keychain,
  sandbox, broker, Store, model, or effect authority. Repair21's subsequent
  official On advanced to revision 29, but cursor-bearing Discord restore could
  not report Connected before the existing typed poll persisted and
  acknowledged the recovery high-water cursor. Repair22 drives that same Host
  poll/ack transaction inside the existing Core-generation fence, while model
  and outbound readiness remain false. It rejects late results after Off/Core
  death, changed pairing, malformed/unexpected recovery, and route-set-invalid
  Mission participation. Focused Rust recovery/Store checks and 56+140 Swift
  tests pass. The complete 238-executed-Rust/two-ignored matrix, release builds,
  strict lint/format, locked metadata, notice closure, plist/scripts, diff/
  conflict, and secret scans also pass. Synchronized final2 passed two fresh
  reviews, was installed through official ServiceManagement/XPC, and restored
  both durable listeners before the later A4 structured-output failure. This
  entry is historical and does not certify Repair23.

- 2026-07-18 Repair23 adds no dependency, provider, recipient, token, Keychain,
  sandbox, broker, Store, model, or effect authority. Repair22 final2 passed
  two fresh reviews, was installed, restored both durable channel listeners,
  and accepted the exact Owner-approved Discord correction once as provider
  message `1528211998263738570`. That dispatch failed before structured-output
  parsing. Read-only inspection of pinned Codex `0.144.0` source commit
  `767822446c7a594caa19609ca435281a9ec67e0d`, the tracked generated schemas,
  and one isolated synthetic non-channel turn proved a passive notification
  compatibility root: OpenOpen named `agentMessage/delta` instead of pinned
  `item/agentMessage/delta` and rejected normal high-reasoning progress events.
  Repair23 accepts only the closed pinned passive progress set with exact
  thread/turn identity and count/byte/item/index bounds. Final authority still
  comes only from the complete `turn/completed` item list; tools, actions,
  reroutes, malformed or unknown methods fail closed. Error details are reduced
  to fixed non-sensitive classes. Both real dispatches remain terminal and are
  never retried. Repair23 final2 was invalidated before installation when its
  fresh product reviewer found that allowed passive metadata did not fully
  reject pinned-schema-malformed active flags and rate-limit fields. Both fresh
  final3 reviewers then invalidated that package before installation with
  P0/P1/P2=`0/0/2`: the pinned `turn/started.items`, lifecycle timestamps,
  bounded item IDs, mandatory type-specific fields, and current package-state
  wording were incomplete. Final4 added those shapes, but its fresh product and
  security reviewers invalidated it before installation with combined
  P0/P1/P2=`0/1/1`: incomplete nested `memoryCitation` validation and a reusable
  Codex transport left alive after malformed/forbidden turn output. Final5
  validates the complete pinned citation shape and funnels every turn-
  collection failure through the existing transport termination path before
  Host reuse. Both fresh final5 reviewers then invalidated it before install
  with P0/P1/P2=`0/1/0`: terminal `itemsView: summary|notLoaded` was accepted
  as a complete authority list and could hide tool/action items. Final6 is
  invalid historical evidence: it omitted optional pinned Turn validation on
  `turn/started`, and its Swift matrix log ended in a missing `swift-format`
  command even though its receipt called that log PASS. Supervisor-approved
  final7 uses one sealed stage-aware gateway for the Turn object returned by
  `turn/start` and both `turn/started` and `turn/completed`; the same path
  validates all known Turn fields, complete accepted nested items, full-item
  authority, stage consistency and sanitized terminal failures. One outer
  boundary retires the transport on every turn-start, collection, inspection,
  parse or output-validation error. The complete current-fingerprint matrix
  passes 253 executed Rust tests with two explicit real-runtime diagnostics
  ignored, 56 broker/signing plus 140 App tests, release builds, strict lint/
  format, locked metadata, notices, plist/scripts, diff/conflict, and secret
  scans. Package and reviewer completion facts are intentionally recorded only
  in an external immutable receipt bound to the exact workspace and package
  fingerprints; this embedded provenance neither self-certifies nor
  dynamically negates them. No provider-success, Mission, Reminder, Receipt,
  Alpha, or competition milestone is claimed.

- 2026-07-19 Repair24 adds no dependency, provider, recipient, credential,
  model, sandbox, broker, or effect authority. Installed Repair23 final7b
  exposed one original OpenOpen product-liveness defect: the same durable
  terminal channel-model failure reopened a blocking modal on every poll.
  Repair24 introduces a Store-owned stable incident identity, atomically
  audited acknowledgement, bounded UI projection over complete durable
  history, non-blocking activity presentation, listener-failure isolation, and
  restart/response-loss handling. It never retries or rewrites either terminal
  dispatch. Incident-scoped acknowledgement feedback/refresh, strict channel
  DTO validation, model-gated Discord setup, live-route-only channel effects
  during account setup, and a required typed poll capability that forbids model
  selection while account/recovery state is not model-ready close the remaining
  product-liveness findings without adding model authority. A fresh pre-freeze
  Product Scout reports P0/P1/P2=`0/0/0`. The eventual frozen Repair24
  fingerprint must bind one wholly successful 269-executed-Rust/two-ignored and
  56+193-Swift matrix, both release builds, warnings-as-errors, strict
  Clippy/rustfmt/swift-format, locked offline metadata, notices, plist/scripts,
  diff/conflict, and bounded secret scanning. Signed package and reviewer facts
  remain external fingerprint-bound evidence and are not self-certified here.
  No new input, provider effect, Mission, Reminder, Receipt, Alpha, or
  competition milestone is claimed.
  The first formal Repair24 Swift run is preserved as historical FAIL evidence:
  one Off-during-poll regression assumed a poll had entered after a fixed 20 ms
  sleep, but whole-suite scheduling had not started it. The test now observes
  the exact poll-entry counter within a bounded deadline before issuing Off;
  five consecutive focused runs pass. The failed log certifies no package or
  later fingerprint.
  The first signing-stage attempt after that matrix also remains historical
  FAIL evidence: the staging script rejected the new App before claiming an
  output because its exact pre/post-signing App/Core Mach-O pins still named
  the preceding build. Fresh temporary-copy signing/removal measurements bind
  the updated App/Core pins; unchanged broker/worker pins were independently
  reproduced. The failed stage created no candidate and certifies no later
  fingerprint.
  After those stage pins were synchronized, the next App staged successfully,
  but its DMG verifier rejected the still-stale App/Core CDHash and unsigned
  table before creating a DMG. Fresh signed-component inspection binds the
  corrected App/Core entries; broker/worker identities remain unchanged. That
  staged App and failed DMG attempt are historical only and certify no later
  fingerprint. The next synchronized package attempt is the third and final
  same-root packaging-pin attempt before supervisor review would be required.
  That next attempt's Rust matrix passed, but its Swift matrix remains
  historical FAIL evidence: one stale-dashboard-generation regression used a
  fixed 10 ms sleep and could start after the newer toggle under full-suite
  load. An isolated supervisor classified the three failed attempts as
  different roots and authorized only a deterministic test-harness repair: the
  two sibling tests must wait for a bounded Mock Core Dashboard-entry counter.
  No production behavior changed, no package was produced, and the repaired
  fingerprint still requires the complete matrix and all package/review gates.

### serenity (implemented exact direct dependency; provider proof pending)

- Upstream: `https://github.com/serenity-rs/serenity`
- Version/tag: `v0.12.5`
- Exact commit: `1809beb0fc24f3942c500058ad4fa47e6a97d3f9`
- License: ISC, copyright 2016 Serenity Contributors
- Allowed surface: official Discord Bot Gateway/HTTP only, with token stored in
  Keychain and least-privilege intents/permissions.
- OpenOpen path: `crates/openopen-discord-adapter/Cargo.toml`; `Cargo.lock`
  records the full exact Git source ending in
  `#1809beb0fc24f3942c500058ad4fa47e6a97d3f9`. The Swift app sends the token
  once from a device-only Keychain item; the adapter probes the official bot
  identity and does not accept user tokens.

Codex app-server schemas are generated from pinned runtime `0.144.0`; the
tracked manifest binds the runtime package components and all 267 schemas. The
runtime binary itself is not stored in Git. Local staging accepts only an
explicit package root whose four component hashes match the manifest. This is
implementation provenance, not redistribution or provider proof. The current
installed Repair19 final5b Developer-ID owner-test build verifies all four
immutable upstream Codex file hashes before signing. It preserves the
OpenAI-signed Codex and code-mode-host
bytes/signatures; only the same upstream linker-ad-hoc `rg` Mach-O is re-signed
for notarization, with a separate content/signature receipt. It is installed
with administrator approval but is not notarized or release proof. Review
status is recorded in external task/PR evidence and is not recursively embedded
into a reviewed package.

`imsg` and serenity are present through the exact adaptation and direct
dependency paths above. imsg's three Swift dependencies are locked by the
tracked `Package.resolved`; serenity is locked by `Cargo.lock`. The current
Friday-alpha distribution notice closure contains 190 OpenOpen and 924 Codex
third-party package identities, 1888 document references, and 597 unique
content-addressed texts; manifest SHA-256 is `818495226dda3332…`. Hero C's
future exact `rust_xlsxwriter 0.96.0` is not distributed and must extend this
closure, provenance map, and notices when implemented. Neither the ad-hoc
history nor the current local Developer-ID
candidate is real provider, notarization, clean-install, or release proof.
